manifest: experiments/hooke/studies/pair_validation/manifest.yaml
run_root: null
device: cuda
job_index: 0
python: python
dry_run: false

hydra:
  job:
    name: hooke-pv-v1
    chdir: false
  run:
    dir: slurm_logs/hooke_pair_validation_v1/single
  sweep:
    dir: slurm_logs/hooke_pair_validation_v1
    subdir: ${hydra.job.num}
