# spechtmp instructions

SpechtMP should use fixed `C` and `B` maps from `spenn/reps`. Learned operations
may mix channels and path indices but must not arbitrarily mix transforming
irrep coordinates.
