# encoding TODO

- Implement order-1 and order-2 features first.
- Enforce symmetric pair features for `(2)` and antisymmetric pair features for `(1,1)`.
- Keep electron-electron cusp as a separate log contribution.
- Defer triples until the pair-only prototype passes tests.
