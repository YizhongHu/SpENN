# configs instructions

Use `configs/config.yaml` as the single active constructor tree. Do not hide
runtime behavior in YAML. The first real milestone should target `M = 2`,
`M_virtual = 2`, Pfaffian readout, Metropolis sampling, and a simple Hamiltonian
before broader config variants are added.
