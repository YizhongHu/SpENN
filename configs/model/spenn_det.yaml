# TODO: Implement determinant readout after the Pfaffian path is stable.
_target_: spenn.nn.wavefunction.SpENNWavefunction
name: spenn_det
description: SpechtMP encoder with determinant readout.
encoder: ${encoder}
spechtmp: ${spechtmp}
readout:
  _target_: spenn.nn.readout.determinant.DeterminantReadout
  implementation_status: pending
cusp:
  enabled: false
