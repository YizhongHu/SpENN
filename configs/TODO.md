# configs TODO

- Keep Hydra group names aligned with `spenn_project_instructions.md`.
- Leave implementation targets skeletal until the corresponding modules exist.
- Keep WandB optional and disabled by default in the skeleton.
- Make any future `M_virtual = 4` behavior explicit opt-in only.
