"""Native PyTorch DDP/DCP spike-local reference package.

The initializer is intentionally inert: capability-gated tests must not import
DCP before they have an opportunity to report an explicit skip.
"""
