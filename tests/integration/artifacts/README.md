# Integration Test Artifacts

This directory stores small, test-owned configs and fixtures used to reproduce
integration-test runs. Generated outputs still go under `outputs/`, not inside
the test tree.
